<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0a0a0a">

    <!-- Top Bar -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="44dp"
        android:orientation="horizontal" android:background="#0f0f0f"
        android:gravity="center_vertical" android:paddingStart="12dp" android:paddingEnd="12dp">

        <ImageButton android:id="@+id/btnBack"
            android:layout_width="32dp" android:layout_height="32dp"
            android:src="@drawable/ic_back"
            android:background="@android:color/transparent"
            android:layout_marginEnd="8dp"/>

        <ImageView android:layout_width="24dp" android:layout_height="24dp"
            android:src="@drawable/ic_hack"
            android:layout_marginEnd="8dp"/>

        <TextView android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="HACKING SUITE"
            android:textColor="#FF4444" android:textStyle="bold"
            android:textSize="14sp" android:fontFamily="monospace"/>

        <TextView android:id="@+id/tvHackStatus"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="● READY"
            android:textColor="#FF4444" android:textSize="10sp"
            android:fontFamily="monospace"/>
    </LinearLayout>

    <!-- Hack Terminal Output -->
    <FrameLayout android:id="@+id/hackTerminalContainer"
        android:layout_width="match_parent" android:layout_height="0dp"
        android:layout_weight="1" android:background="#0a0a0a"/>

    <!-- Input Bar -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="50dp"
        android:orientation="horizontal" android:background="#0f0f0f"
        android:gravity="center_vertical" android:paddingStart="8dp" android:paddingEnd="8dp">

        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="root@vpr:~# "
            android:textColor="#FF4444" android:textSize="12sp"
            android:fontFamily="monospace"/>

        <EditText android:id="@+id/hackInput"
            android:layout_width="0dp" android:layout_height="match_parent"
            android:layout_weight="1" android:background="@null"
            android:textColor="#FF4444" android:textColorHint="#4d1a1a"
            android:hint="scan / recon / cve / xss / sqli / dirbr..."
            android:textSize="12sp" android:fontFamily="monospace"
            android:imeOptions="actionSend" android:singleLine="true"
            android:inputType="text"/>

        <Button android:id="@+id/btnHackRun"
            android:layout_width="wrap_content" android:layout_height="36dp"
            android:text="▶ EXEC" android:textSize="11sp"
            android:textColor="#000000" android:backgroundTint="#FF4444"
            android:paddingStart="10dp" android:paddingEnd="10dp"
            android:layout_marginStart="4dp"/>
    </LinearLayout>
</LinearLayout>
